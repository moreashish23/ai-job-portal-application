server:
  port: 5003

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job_portal_Job_DB
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver